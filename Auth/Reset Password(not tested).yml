info:
  name: Reset Password(not tested)
  type: http
  seq: 5

http:
  method: POST
  url: http://127.0.0.1:8000/api/reset-password
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "okta@warung.com",
          "password": "passwordBaru123",
          "password_confirmation": "passwordBaru123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
