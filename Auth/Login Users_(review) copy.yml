info:
  name: Login Users_(review) copy
  type: http
  seq: 8

http:
  method: POST
  url: https://backend-lomba-php-production.up.railway.app/api/login
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "oktaokta2@warung.com",
          "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
