info:
  name: Login Users_(review)
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
          "email": "oktaokta5@warung.com",
          "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
