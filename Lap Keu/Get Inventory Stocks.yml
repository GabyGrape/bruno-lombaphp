info:
  name: Get Inventory Stocks
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/api/reports/finance-summary?user_id=1
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer
  params:
    - name: user_id
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
