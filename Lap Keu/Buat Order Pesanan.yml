info:
  name: Buat Order Pesanan
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/api/orders
  body:
    type: json
    data: |-
      {
        "merchant_id": 1,
        "user_id": 2,
        "items": [
          {
            "product_id": 1, 
            "quantity": 2
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
